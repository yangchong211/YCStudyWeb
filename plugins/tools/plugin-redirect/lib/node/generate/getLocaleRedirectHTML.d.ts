import type { RedirectLocaleConfig } from '../../shared/index.js';
export declare const getLocaleRedirectHTML: ({ localeConfig, defaultBehavior, defaultLocale, localeFallback, }: RedirectLocaleConfig, availableLocales: string[], base: string) => string;
